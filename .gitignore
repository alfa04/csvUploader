# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
.venv/
venv/

# uv
# (uv.lock IS committed — do not ignore it)

# Lambda build artifacts
build/
*.zip

# Terraform
**/.terraform/
*.tfstate
*.tfstate.*
*.tfplan
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
# .terraform.lock.hcl IS committed — it pins exact provider versions/hashes, do not ignore it

# Environment / secrets
.env
.env.*
*.pem
*.key

# OS / editor
.DS_Store
.vscode/
.idea/

# Claude Code (nested worktrees live under here and are not part of this repo's tree)
.claude/
